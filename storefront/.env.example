# Environment Configuration
NODE_ENV=development

# API Configuration
# This is the single entry point for all microservice requests.
# In development, it points to the Kong Gateway (Port 8000).
API_URL=http://localhost:8000

# Session Configuration
SESSION_SECRET=dev-session-secret-change-me-in-production

# Application Configuration
# This is the public URL of the frontend application itself.
# Used for canonical SEO links and absolute redirects.
APP_NAME=WebApp
APP_URL=http://localhost:3000
