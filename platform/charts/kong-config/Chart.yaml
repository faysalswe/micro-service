apiVersion: v2
name: kong-config
description: Kong KongPlugin, KongConsumer, and JWT credential resources (configures the Kong proxy installed by the upstream kong/kong chart)
type: application
version: 0.1.0
