apiVersion: v1
kind: Service
metadata:
  name: pdf-service
  labels:
    app: pdf-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: pdf-service
