apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: microservices
  labels:
    app: order-service
spec:
  type: ClusterIP
  ports:
    - port: 5011
      targetPort: 5011
      protocol: TCP
      name: grpc
  selector:
    app: order-service
